{
  "name": "json-server-mealplan",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "npm run build && cross-env NODE_ENV=test jest && npm run lint",
    "start": "node server.js",
    "json-server": "json-server db.json --middlewares ./auth-middleware.js --routes ./routes.json",
    "heroku-prebuild": "npm install"
  },
  "author": "Inpyo Hong <y2kpaulh@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "json-server" : "^0.17.0"
  }
}
